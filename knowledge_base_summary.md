# 番茄病害诊治系统知识库位置与结构

## 1. 诊断知识库
**文件位置**: `c:\test\diagnosis_model.py`

### 1.1 病害类别定义
```python
DISEASE_CLASSES = [
    "健康", "早疫病", "晚疫病", "黄化曲叶病毒病", 
    "叶霉病", "白粉病", "细菌性斑点病", "灰霉病"
]
```

### 1.2 症状-病害映射知识库
```python
SYMPTOM_TO_DISEASE = {
    "斑点": ["早疫病", "晚疫病", "细菌性斑点病"],
    "发黄": ["黄化曲叶病毒病", "早疫病"],
    "腐烂": ["晚疫病", "灰霉病"],
    "白粉": ["白粉病", "叶霉病"],
    "卷曲": ["黄化曲叶病毒病"],
    "枯萎": ["早疫病", "晚疫病"],
    "霉斑": ["叶霉病", "灰霉病"],
}
```

### 1.3 病害描述知识库
在 `_get_disease_description` 方法中定义：
```python
descriptions = {
    "早疫病": "早疫病是番茄常见真菌性病害，在叶片上形成同心轮纹状病斑，边缘有黄色晕圈。",
    "晚疫病": "晚疫病会导致番茄果实和叶片快速腐烂，病斑呈水渍状，在潮湿环境下发展迅速。",
    "黄化曲叶病毒病": "由白粉虱传播的病毒病，导致叶片黄化、卷曲、变小，植株生长受阻。",
    # ... 其他病害描述
}
```

### 1.4 规则诊断知识库
在 `_rule_based_diagnosis` 方法中定义：
```python
disease_knowledge = {
    "番茄": {
        "斑点": ("早疫病", 0.88, "早疫病在叶片上形成同心轮纹状病斑，边缘有黄色晕圈"),
        "叶子发黄": ("黄化曲叶病毒病", 0.80, "由白粉虱传播的病毒病，导致叶片黄化卷曲"),
        # ... 其他规则
    }
}
```

## 2. 治疗方案知识库
**文件位置**: `c:\test\agents.py`

在 `_get_treatment_from_knowledge_base` 函数中定义：
```python
treatment_knowledge = {
    "早疫病": {
        "treatment": "1. 发病初期：使用百菌清、代森锰锌或嘧菌酯喷雾...",
        "prevention": "1. 轮作倒茬，避免连作 2. 加强栽培管理，合理密植..."
    },
    "晚疫病": {
        "treatment": "1. 发病初期：使用烯酰吗啉、霜脲氰或氟吡菌胺喷雾...",
        "prevention": "1. 选用抗病品种 2. 避免密植，增加通风透光..."
    },
    # ... 其他病害的治疗方案
}
```

## 3. 辅助知识库文件
**文件位置**: `c:\test\tomato\integrate_with_system.py`

该文件包含病害类别转换和更新逻辑，用于将训练模型的类别名称转换为中文并更新到主知识库中。

## 4. 知识库使用流程

1. **信息收集**：`reception_agent` 从用户输入中提取作物类型、症状、生长阶段等信息
2. **病害诊断**：`diagnosis_agent` 使用 `diagnosis_model.py` 中的知识库进行病害诊断
3. **治疗方案生成**：`treatment_agent` 使用 `agents.py` 中的知识库生成治疗方案
4. **流程控制**：`supervisor_agent` 协调各智能体的工作流程

## 5. 扩展知识库

如需添加新的病害或修改现有信息，可以：

1. 在 `diagnosis_model.py` 中更新 `DISEASE_CLASSES`、`SYMPTOM_TO_DISEASE` 和 `descriptions`
2. 在 `agents.py` 中更新 `treatment_knowledge`
3. 使用 `tomato\integrate_with_system.py` 工具更新模型类别

## 6. 混合诊断方式

系统采用**规则引擎与深度学习模型结合**的混合诊断方式：
- 基于症状的诊断主要使用规则知识库
- 基于图像的诊断主要使用深度学习模型
- 两种方式结合提高诊断准确率和置信度